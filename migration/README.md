# Migration from MySQL to CouchDB

1. Install pip

		python get-pip.py

2. Install mysql

		./setup.sh

3. Change IP_ADDRESS, USERNAME, PASSWORD accordingly in `migrate.py`, then run:

		python migrate.py

